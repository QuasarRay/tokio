[package]
name = "tokio-verification-macros"
version = "0.0.0"
edition = "2021"
rust-version = "1.71"
publish = false
license = "MIT"

[lib]
proc-macro = true

[dependencies]
proc-macro2 = "=1.0.78"
quote = "=1.0.35"
syn = { version = "=2.0.48", features = ["full", "parsing"] }

[lints.rust]
unsafe_code = "forbid"
